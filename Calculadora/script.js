let number = ""
let results = document.querySelector(".results")

function AddCharacters(character) {
    results.innerHTML += character

    /* number += character */ /* pegando os caracteres */
}

function Calculate() {
    /* resultsText = parseFloat(document.querySelector(".results").textContent)
    resultsValue = resultsText
    console.log(resultsText)
    console.log(resultsValue) */
}
